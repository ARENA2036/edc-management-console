# Test-only dependencies.
#
# Kept out of requirements.txt so nothing here is installed into the runtime
# image: the Dockerfile installs requirements.txt alone. CI installs both.

pytest==9.1.1
pytest-asyncio==1.3.0

# Used by fastapi.testclient.TestClient, which the API tests will need.
httpx==0.28.1
